<p id="notice"><%= notice %></p>

<p>
  <b>Cart:</b>
  <%= @line_item.cart_id %>
</p>

<p>
  <b>Product:</b>
  <%= @line_item.product_id %>
</p>

<p>
  <b>Quantity:</b>
  <%= @line_item.quantity %>
</p>


<%= link_to 'Edit', @nested ? edit_cart_line_item_path(@line_item.cart_id, @line_item.id) : edit_line_item_path(@line_item) %> |
<%= link_to 'Back', @nested ? cart_path(@line_item.cart_id) : line_items_path %>
